function log(output) {
    console.log(output);
}

function warn(output) {
    console.warn(output);
}

function error(output) {
    console.error(output);
}

module.exports = {
    log,
    warn,
    error
};
